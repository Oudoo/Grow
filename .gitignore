# Dependencies
node_modules

# Build output
# .next
dist
build
out
coverage
*.tsbuildinfo

# Generated Prisma clients (regenerated on build via `prisma generate`)
**/src/generated

# Local databases (regenerated via db:push + seed)
*.db
*.db-journal

# Environment — never commit secrets
.env
.env.local
.env.*.local

# Logs & OS
*.log
.DS_Store

# Editor
.idea

# Local MCP config (contains API token — never commit)
.mcp.json
