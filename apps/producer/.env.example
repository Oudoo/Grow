# ── Growees Producer — environment ──
# SQLite file DB. On Hostinger it lives in the app's nodejs directory and is
# re-seeded with demo data on first boot (see scripts/start.mjs).
DATABASE_URL="file:./production.db"
NODE_ENV=production
